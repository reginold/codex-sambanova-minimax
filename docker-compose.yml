services:
  postgres:
    container_name: sambanova-postgres-1
    image: postgres:16
    environment:
      POSTGRES_USER: litellm
      POSTGRES_PASSWORD: litellm
      POSTGRES_DB: litellm
    ports:
      - "5433:5432"
    volumes:
      - litellm-sambanova_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U litellm"]
      interval: 5s
      timeout: 5s
      retries: 5

  litellm:
    container_name: sambanova-litellm-1
    image: ghcr.io/berriai/litellm:main-latest
    environment:
      SAMBANOVA_API_KEY: ${SAMBANOVA_API_KEY}
      LITELLM_MASTER_KEY: ${LITELLM_MASTER_KEY}
      DATABASE_URL: postgresql://litellm:litellm@postgres:5432/litellm
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "4000:4000"
    volumes:
      - ./proxy_config.yaml:/app/proxy_config.yaml:ro
    command: ["--host", "0.0.0.0", "--port", "4000", "--config", "/app/proxy_config.yaml"]

volumes:
  litellm-sambanova_pgdata:
    external: true
